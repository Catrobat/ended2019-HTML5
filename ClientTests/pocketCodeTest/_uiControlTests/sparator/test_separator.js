

window.onload = function () {
  var layoutContainer = document.getElementById('layoutContainer');

  var separator = new PocketCode.Ui.MenuSeparator();
  layoutContainer.appendChild(separator._dom);
};